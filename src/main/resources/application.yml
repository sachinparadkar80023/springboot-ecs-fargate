server:
  port: 8080

spring:
  application:
    name: springboot-ecs-fargate

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
